<Window x:Class="MedlemsApp.Views.MedlemProfilView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedlemsApp.Views"
        mc:Ignorable="d"
        Title="MedlemProfilView" Height="650" Width="800"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5" ResizeMode="NoResize">
    <Grid>
        <Border Height="120" VerticalAlignment="Top" Background="#2C3E50">
            <TextBlock Text="Inställningar" Foreground="White" FontSize="26" 
                       FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Margin="30,80,30,20" Background="White" CornerRadius="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>

            <StackPanel Margin="25">
                <Grid HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Ellipse Width="100" Height="100" Fill="#E9ECEF" Stroke="#DEE2E6" StrokeThickness="2"/>
                    <TextBlock Text="👤" FontSize="50" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#ADB5BD"/>
                    <Button Command="{Binding BytBildCommand}" Content="📷" Width="30" Height="30" 
                            VerticalAlignment="Bottom" HorizontalAlignment="Right" 
                            Background="#3498DB" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>

                <TextBlock Text="{Binding InloggadMedlem.Namn}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding InloggadMedlem.MedlemskapsNivå, StringFormat='Nivå: {0}'}" Foreground="#6C757D" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <Separator Margin="0,0,0,20" Background="#F1F3F5"/>

                <TextBlock Text="E-post" FontSize="12" FontWeight="Bold" Foreground="#495057" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email}" Height="35" Margin="0,0,0,15" Padding="10,5" VerticalContentAlignment="Center" BorderBrush="#DEE2E6"/>

                <TextBlock Text="Telefonnummer" FontSize="12" FontWeight="Bold" Foreground="#495057" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Telefon}" Height="35" Margin="0,0,0,15" Padding="10,5" VerticalContentAlignment="Center" BorderBrush="#DEE2E6"/>

                <TextBlock Text="Nytt Lösenord (lämna tomt för att behålla nuvarande)" FontSize="12" FontWeight="Bold" Foreground="#495057" Margin="0,0,0,5"/>
                <PasswordBox Height="35" Margin="0,0,0,30" Padding="10,5" VerticalContentAlignment="Center" BorderBrush="#DEE2E6"/>

                <Button Content="Spara ändringar" Command="{Binding SparaCommand}" 
                        Height="45" Background="#27AE60" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
